server:
  port: ${PORT:8080}

spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:my_ecommerce}?useSSL=true&sslMode=REQUIRED&serverTimezone=UTC
    username: ${DB_USER:root}
    password: ${DB_PASSWORD:970Teja@}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:465}
    username: ${MAIL_USERNAME:teamvelocity4you@gmail.com}
    password: ${MAIL_PASSWORD:siykurjgrcjydedy}
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
          socketFactory:
            port: 465
            class: javax.net.ssl.SSLSocketFactory
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:https://sarees-for-naaris.vercel.app,http://localhost:5173}

app:
  jwt:
    secret: ${JWT_SECRET:dGhpcy1pcy1hLXNlY3JldC1rZXktZm9yLXNhcmVlcy1mb3ItbmFhcmlzLWF1dGgtc2VydmljZS1zaG91bGQtYmUtYXQtbGVhc3QtMjU2LWJpdHMtbG9uZw==}
    accessTokenExpirationMs: ${JWT_ACCESS_EXPIRATION:3600000}
    refreshTokenExpirationMs: ${JWT_REFRESH_EXPIRATION:604800000}
  otp:
    expirationMinutes: 10
  huggingface:
    apiKey: ${HUGGINGFACE_API_KEY:your_huggingface_api_key_here}
    modelId: ${HUGGINGFACE_MODEL_ID:mistralai/Mistral-7B-Instruct-v0.3}


razorpay:
  key-id: ${RAZORPAY_KEY_ID:rzp_test_TKRCcwoUzNdkk5}
  key-secret: ${RAZORPAY_KEY_SECRET:HjTAF8bl7toiKMqjjgJtGAFZ}
